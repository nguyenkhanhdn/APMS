@model IEnumerable<APMS.Models.ParkingSlot>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SlotNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SlotNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ParkingSlotId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ParkingSlotId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ParkingSlotId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
